import 'package:flutter/material.dart';

class ButtonStyles {
  static var shadowPadding = ElevatedButton.styleFrom(
    shadowColor: Colors.deepPurple[300],
    elevation: 10,
    padding: const EdgeInsets.symmetric(horizontal: 20.0, vertical: 15.0),
  );
}
